---
- hosts: localhost
  remote_user: travis
  sudo: true

  roles:
    - role: ansible-ferm
      tags: [app, ferm]
      ferm_input_list:
        - type: "dport_accept"
          dport: ["http"]
          filename: "nginx_accept"